displayName: minimal-endpoint
type: Che Plugin
spec:
  endpoints:
    - name: www
      targetPort: 3100
      path: /hello
  containers:
    - name: minimal-endpoint
      image: 'quay.io/minimal-endpoint'
      command:
       - foo
      args:
       - bar
    
